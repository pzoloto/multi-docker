name: Deploy MultiDocker
on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t philleopolis/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true philleopolis/react-test npm test

      - run: docker build -t philleopolis/multi-client ./client
      - run: docker build -t philleopolis/multi-nginx ./nginx
      - run: docker build -t philleopolis/multi-server ./server
      - run: docker build -t philleopolis/multi-worker ./worker

      - run: docker push philleopolis/multi-client
      - run: docker push philleopolis/multi-nginx
      - run: docker push philleopolis/multi-server
      - run: docker push philleopolis/multi-worker
